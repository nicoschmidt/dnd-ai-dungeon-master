"""The rules core: pure, deterministic domain logic.

Dice arithmetic, attack resolution, conditions, state transitions. Imports
neither a web framework nor the Claude Agent SDK, and makes no model calls.
"""
